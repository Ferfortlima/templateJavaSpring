template:

  #Spring
  spring:
    jpa:
      hibernate:
        ddl-auto: create
      properties:
        hibernate:
          format_sql: true
      show-sql: true
    datasource:
      url: jdbc:h2:mem:testdb
      username: sa
      password:
    h2:
      console:
        enabled: true
        path: /h2-console

server:
  port: 8445
  compression:
    enabled: true


